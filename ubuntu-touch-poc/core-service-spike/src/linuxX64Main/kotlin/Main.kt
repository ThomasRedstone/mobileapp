// Phase 0 spike 1: confirms Kotlin/Native linux toolchain builds and links a
// trivial binary in this environment. Running it on real Ubuntu Touch
// (Halium) hardware is a separate, unverified step — see
// docs/ubuntu-touch-poc-plan.md.
fun main() {
    println("core-service-spike: kotlin/native linux toolchain OK")
}
