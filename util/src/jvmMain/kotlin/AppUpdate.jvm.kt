package coredevices.coreapp.util

// Desktop Linux/Ubuntu Touch has no in-app update mechanism (updates are handled by the
// system package manager), so there is never anything to carry here.
actual data object AppUpdatePlatformContent
